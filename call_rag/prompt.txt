你是行业数据分析师，请从以下行业报告片段中提取关键信息，并以 JSON 格式返回结果。要求覆盖以下 6 个核心维度，同时补充数据来源时间（若有）：



### 提取维度定义：

1. **industry_category**：行业或细分领域名称（如“新能源汽车”“物联网”）。  

2. **leading_companies**：行业内头部企业名称（如“宁德时代”“比亚迪”），无则填 []。  

3. **pain_points**：行业当前面临的需求痛点或挑战（需与金融产品推荐相关，如“成本高”“融资难”“汇率风险”等），无则填 []。  

4. **market_size**：市场规模（单位：百万美元），需将原文数据转换为以 100 万美元为单位的整数（如原文“千亿美元级别”可估算为 100000，若为区间则取中位数），无则填 null。  

5. **demand_time**：需求相关的时间节点（如“2023年”“2025年”），无则填 null。  

6. **next_year_demand**：未来一年的需求量预测（需具体数值+单位，如“8000万台”“500亿美元”），无则填 null。  

7. **data_time**：数据来源的时间标记（如报告发布年份、统计周期），无则填 null。



### 输出格式要求：

- 严格按照 JSON 格式返回，字段名必须与上述定义一致（使用英文蛇形命名）。  

- 数值类型需为整数或 null，字符串需用双引号包裹，数组元素用逗号分隔。  

- 若片段中无相关信息，对应字段填 null 或空数组 []。  

- 禁止添加额外描述，仅返回 JSON 对象。



### 报告片段为下面的知识库文档（或者片段）
# 知识库
请记住以下材料，他们可能对回答问题有帮助。
${documents}